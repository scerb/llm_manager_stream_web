Model: granite-3.2-8b-instruct-Q4_K_M
Request ID: 55
Timestamp: 2025-08-28T11-34-36

Question:
Design a JSON Schema for invoices (vendor, invoice_date ISO-8601, line_items[], total_amount); include validation rules.

Answer:
Here is a JSON Schema for invoices with validation rules:

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Invoice",
  "description": "Schema for invoice data",
  "type": "object",
  "properties": {
    "vendor": {
      "type": "string",
      "minLength": 1,
      "maxLength": 100,
      "pattern": "^[a-zA-Z0-9 ]+$"
    },
    "invoice_date": {
      "type": "string",
      "format": "date-time",
      "pattern": "^([0-9]{4})-(0[1-9]|1[0-2])-(0[1-9]|[12][0-9]|3[01])T([01][0-9]|2[0-3]):([0-5][0-9]):([0-5][0-9])\.([0-9]+)$"
    },
    "line_items": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "product_name": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100,
            "pattern": "^[a-zA-Z0-9 ]+$"
          },
          "quantity": {
            "type": "integer",
            "minimum": 0
          },
          "price": {
            "type": "number",
            "minimum": 0
          }
        },
        "required": ["product_name", "quantity", "price"]
      }
    },
    "total_amount": {
      "type": "number",
      "minimum": 0
    }
  },
  "required": ["vendor", "invoice_date", "line_items", "total_amount"]
}
```

This JSON Schema defines the structure and validation rules for an invoice object. Here's a breakdown of the schema:

- `vendor`: A string representing the vendor's name, with a minimum length of 1 and a maximum length of 100. It should only contain alphanumeric characters and spaces.

- `invoice_date`: A date-time string in ISO 8601 format, with a specific pattern to
